import { Button } from '@common/components'

// <Button onClick={() => sendMyStatement('Harald Töpfer')}>Send statement</Button>
const XapiTest = () => {
  return <Button id={'test-button'}>Test</Button>
}

export default XapiTest
