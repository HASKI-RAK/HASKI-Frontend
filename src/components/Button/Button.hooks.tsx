const useButton = () => {
  // const fetchUser = usePersistedStore((state) => state.fetchUser) // In den Button auslagern? Muss in einer Komponente liegen, da Hook
  /*await fetchUser()
        .then((user) => {
          return user.lms_user_id.toString()
        })
        .catch((error) => {
          log.error(error)
          return '-1'
        })*/
}

// Was noch zu klören ist, ob das komplette Statement oder nur die einzelnen Teile aus dem Hook kommen
// -> also nicht getActor sondern getURL oder sowas
